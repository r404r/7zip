# B04 — extraction safety qualification (prerequisite stage only)

Status: NOT QUALIFIED. No archive has been generated or executed by this harness.
`hostile_execution_authorized` and `b04_complete` are unconditionally false.
An exit-zero prerequisite run is **not** B04 PASS or permission to extract.

## Approved stage and review contract

Card `t_2a64c953` records human authorization to add only the B04 owned directory,
this document, and `.github/workflows/b04-native.yml`. First implement and run
candidate isolation controls, then request independent review of an exact commit.
The reviewer must record a **prerequisite-only** verdict and return this same card
to tester for continuation, NOT complete the card or release B06/S4/S8. No merge
into a shared branch, new spending, host security relaxation, production edits,
golden replacement, or release is authorized.

## Runnable artifact

From an isolated checkout, with a native C compiler (MSVC developer environment
on Windows), Python 3.11+, and bubblewrap on Linux:

```
python .github/tests/migration/b04/test_prerequisite.py
python .github/tests/migration/b04/prerequisite.py <new-evidence-directory>
```

The directory must not exist and its path must be ASCII. This restriction is a
probe implementation limitation, not an archive filename compatibility rule.
The runner builds only `probe.c` (plus `winlaunch.c` on Windows), using warnings
as errors. It does not build, change, or invoke the archive engine. There is no
arbitrary executable/command/archive option and no persisted approval flag.
The separate workflow runs only on `wt/t_2a64c953`; failed controls fail the job
with exit 2 and still upload raw evidence. It does not change existing workflows.

## Threat boundary and candidate implementations

The trusted parent creates separate baseline and sandbox envelopes. Every target
is a new disposable control file. The unconfined baseline deliberately modifies
only its own controls and proves those operations actually work; it is not a
fallback after sandbox failure. The sandbox probe tries the same bounded writes.
No existing user file is read or written as a control. All probes have empty or
minimal environments, closed inherited descriptors, and null stdin. The Windows
AppContainer child inherits no handles and an explicitly empty environment.

- Linux: bubblewrap unshares namespaces, drops all capabilities, starts a new
  session, binds system runtime and the probe read-only, and places output in
  fresh tmpfs. **No host directory is writable-mounted.** The outside controls
  have a separate read-only mount. This deliberately avoids the well-known
  preexisting-hardlink hole of binding a host output directory writable: a
  read-only alias elsewhere does not protect an inode reached through a writable
  bind. A fresh tmpfs cannot contain incoming external inode aliases. No `/proc`,
  host home, credentials, host `/tmp`, or device tree is mounted. The probe's raw
  stdout is captured by the parent; there is no host-backed output tree.
- macOS: native `sandbox-exec`, deny by default; read access to the explicit
  system runtime and disposable envelope, writes only to the resolved work path.
  No broad `mach-lookup`, network, or process-fork allowance. This is an empirical
  candidate, not an assertion that path rules correctly contain hardlink/reparse
  races. The exact `.sb` is retained. Deprecation/OS differences may reject it.
- Windows: native AppContainer via `PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES`,
  a derived SID without creating a profile, zero capabilities, no fallback.
  Only the disposable envelope gets SID read/execute rights; its work directory
  gets write rights and a low-integrity label. No global ACL, privilege, developer
  mode, security policy, or AppContainer profile is changed. A static CRT probe
  avoids granting access to a user-installed Python runtime. Win32 errors and
  build output are retained. The fixed test SID is not a claim of inter-run
  adversarial isolation; concurrent untrusted processes are not authorized.

These are bounded file-write candidates, **not** general malicious-program
sandboxes. Network denial, IPC, registry, resource limits, descendant cleanup,
preexisting aliases, reparse variants and active races require further native
controls and review before any hostile archive. The fixed native probe spawns
no descendants. Windows launcher timeout termination is failure, never graceful
cancellation evidence.

## Current control relations (not invented legacy goldens)

`controls_pass` requires all expected keys, both exit codes zero, all baseline
operations successful, and all sandbox outside writes denied with sentinel bytes
unchanged. Missing/malformed output, compiler/launcher failure, unsupported native
platform or any inside operation failure means not qualified.

Inside controls: file creation, rename, hardlink creation/write, symlink
creation/write, and refusal to write a read-only file. These must also succeed
in the unconfined control (including the intentional refusal). This detects
coarse isolation that destroys the link or simple permission behavior of interest.

Outside controls: absolute-path write, `../outside` traversal, an absolute
symlink followed by write, and creating then writing a hardlink alias to an
outside sentinel. The raw post-expression errno/Win32 last error is diagnostic;
it is not meaningful on a successful expression and does not distinguish all
short-circuited substeps. No error is relabelled as an archive HRESULT.

Limitations: Linux uses tmpfs rather than a host archive target volume; EXDEV
for external hardlink creation is a mount-boundary control, not retained-engine
hardlink policy. Passing inside checks establishes only these elementary native
operations, not general ACL parity. It cannot qualify Windows reparse semantics,
full metadata fidelity, network shares, or cross-volume engine outcomes.

The unit tests include missing-evidence rejection and temporary in-memory
mutations of the acceptance relations; those dictionaries test validator logic,
not archive golden data. A native end-to-end test confirms that both success and
safe rejection retain a report with archive authorization false. That test may
pass on a rejected candidate; the separate capture invocation must return zero
for the CI candidate job to pass.

## Evidence and remaining qualification

Local initial Linux execution: `cc -std=c11 -Wall -Wextra -Werror` build and
three unittest tests passed. Baseline operations all returned true; sandbox
inside controls all returned true; outside writes all returned false. Raw Linux
outside diagnostics were EROFS (30) for direct/traversal/symlink paths and EXDEV
(18) for hardlink creation. All sandbox sentinels remained byte-identical.
These observations were collected in `work/local-1/`, not manufactured expected
archive data. Subsequent native run IDs and immutable evidence are recorded on
the card and below when available.

### Native candidate execution and rejection

Capture commit: `5dee62a1a526063374f332370269dcadab9c4168`.
[Native run 34569823142](https://github.com/r404r/7zip/actions/runs/34569823142)
completed with **failure**. All three native C builds and all three unittest
suites succeeded, but none of the hosted candidates qualified:

| Native platform | Observed sandbox result | Interpretation |
| --- | --- | --- |
| Ubuntu hosted | exit 1, `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted` | Namespace/network setup rejected before probe execution; no boundary evidence |
| macOS 26.6.2 arm64 | return code -6, empty stdout/stderr | Candidate aborted before any control record; cause not established |
| Windows Server 2025 | launcher exit 1, `AppContainer candidate result=2`, empty child log | Candidate launch/setup failed; exact failing API not yet instrumented |

All unconfined native baselines produced all nine records successfully; all
sandbox sentinel bytes were unchanged. **Unchanged sentinels with a failed launch
are not containment proof.** The validator correctly refused each candidate.
The Linux local repeat at the same capture commit succeeded with the same nine
bounded relations, but cannot substitute for native hosted CI qualification.

Byte-preserved reports, exact command arrays, compiler stdout/stderr, probe
stdout/stderr, Windows child logs, macOS profile and run/job metadata are under
`.github/tests/migration/b04/evidence/34569823142/`. The same-commit local repeat
is in `evidence/local-linux/`. `evidence/MANIFEST.sha256` inventories captured
bytes; run `sha256sum -c .github/tests/migration/b04/evidence/MANIFEST.sha256`
from the repository root. Native CRLF bytes and native source hashes are retained
unchanged; the Windows checkout representation differs from Unix. These files
are failed/successful prerequisite observations, not extraction goldens.

The unchanged existing workflows at that commit succeeded:
`migration-native-cli` (34569823044), `characterization-native` (34569823145),
and `build-windows-x64` (34569823077). They do not qualify these new sandboxes.

Next step is independent **candidate/negative-evidence review**, not a safety
PASS. Further same-card engineering must diagnose the native failures without
relaxing host policy or weakening the control relations. In particular, Microsoft's
[AppContainer implementation guidance](https://learn.microsoft.com/en-us/windows/win32/secauthz/implementing-an-appcontainer)
requires a profile and describes writable per-user storage outside this candidate's
single work root. The profileless candidate's suitability is unproven; merely
adding a profile is not a valid root-containment fix. A restricted-token candidate
or a separately reviewed disposable-storage boundary needs actual evidence.
Do not infer the precise cause of error 2 from this documentation alone. macOS
needs a targeted startup diagnostic, not broad file-read/IPC allowances. Ubuntu
must not be repaired by disabling AppArmor, sharing the host network, or otherwise
relaxing host policy. No such workaround has been applied.

Still required before full B04: independently reviewed safe execution boundary;
actual native Windows/Linux/macOS containment and negative controls including
races/reparse/escape attempts; a reviewed extraction and result-collection path;
then unchanged legacy tree/payload/hash/metadata/outcome captures for hostile
names, collisions, read-only/permission failures, interactive overwrite replies,
EOF/cancel, null streams and partial effects. Prompt cancellation is not timeout
kill. No fixture or legacy expectation exists in this stage. Unsupported evidence
must remain a gate, never silently skipped as PASS.

M2 retains `Extract` / `ArchiveExtractCallback` and overwrite/path policy. No Rust
sanitizer, transactional rollback guarantee, production policy, delete behavior,
codec, GUI, or encryption change belongs to this test infrastructure.

### Diagnostic continuation (not qualified)

Diagnostic continuation after prerequisite review CHANGES: the runner now records
a fixed `/usr/bin/true` launch under the **identical** Unix sandbox policy and
mounts before the bounded probe. This separates startup failures from probe
operations; its outcome is never substituted for any acceptance relation.
Windows records the immediate Win32 error or HRESULT and API at setup/launch
failure, and distinguishes child exit, wait failure and timeout. The termination
wait is bounded rather than infinite. A native nonexistent-envelope control must
be rejected at `GetNamedSecurityInfoW` without launching a child. These diagnostic
changes do not qualify the boundaries or add profile/ACL/IPC/network allowances.
Additional native evidence is required; unexercised timeout/API error branches
remain coverage gaps, not claimed compatibility tests.

Capture commit `2b18c183d3a49dcef65ad1c4f4f1d507c68e7e9f`, native run
[34572155278](https://github.com/r404r/7zip/actions/runs/34572155278), again failed
all three candidate jobs. Native builds and four tests per platform passed.
Windows now pinpoints `B04-launch stage=launch api=CreateProcessW error=2`;
the separate missing-envelope control records
`B04-launch stage=setup api=GetNamedSecurityInfoW error=2` and passes its rejection
test. The two errors are no longer conflated. The underlying missing object in
`CreateProcessW` remains unidentified; a missing profile is not proven by error 2.
Linux's fixed startup control and probe both exit 1 with
`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`. macOS's fixed
startup control and probe both return -6 with empty stdout/stderr. Thus neither
failure depends on probe file operations; this does not identify the specific
macOS loader/sandbox denial. All sandbox control maps remain empty, baselines
are successful, and sentinels unchanged. No hostile archive was executed.

Raw downloaded bytes and run metadata are frozen under
`.github/tests/migration/b04/evidence/34572155278/`, with a separate
`evidence/DIAGNOSTIC-MANIFEST.sha256`; the earlier evidence is untouched.
The same-commit local Linux diagnostic run succeeded (including the fixed startup
control), at `work/diagnostic-local-45/report.json`, but is not native CI parity.
This second native attempt resolves diagnostic ambiguity, not the substantive
isolation failure. Stop under the two-failure escalation limit. Human decision
is required before another candidate iteration or changing the disposable storage
boundary. No safety prerequisite review PASS or full B04 PASS is claimed; these
diagnostic changes themselves still require independent review after the gate is
resolved. B06/S4/S8 remain gated.

### Preparation after explicit one-round authorization (not executed)

The operator subsequently approved "批准 B04 一次有界诊断" on this same card.
This is the one process-sandbox startup round specified by the independently
reviewed [B04-P recovery report](b04-actions-recovery-plan.md), not permission
for hostile extraction or another sequence of retries. Reviewed report commit
`9c038dc7ed89034ac0b2c14c4d12bec2e6eed7ca` was consumed by ordinary cherry-pick
`5fb8282` into `wt/t_2a64c953`. M3 `0706af3` remains an ancestor. Source checkout
is still `ai/migration-bootstrap-20260911`; both copies of AGENTS.md were read.

This handoff is **pre-execution static review** of `9728808` plus the precise new
delta. It must NOT complete B04, integrate its implementation into the automation
base, or release B06/S4/S8. On stage PASS, record the exact reviewed commit and
return this same card to the original tester for the already-authorized one
dispatch; no repeated human approval is required for that same budget. Correctable
defects use same-card CHANGES, at most one revision in this newly approved phase;
two substantive review failures escalate. Historical native failures are retained.

The prepared entry is `.github/tests/migration/b04/diagnostic.py`. It deliberately
does not accept arbitrary commands, archives or a destination argument. It calls
the retained `capture` with a bounded runner and a fixed observation hook. The old
`prerequisite.py` CLI and the optional `test_prerequisite.py --native` are NOT
approved entries for this round. Default tests are now offline; the old real
capture test is retained behind explicit `--native`, and that switch is absent
from CI. This is separation of execution budget, not a native PASS or removal of
qualification requirements.

Prepared behavior:

- Ref/run guard requires the actual checked-out full SHA, manual event, public
  target repository workflow guard, exact B04 branch, first run attempt and the
  explicitly supplied reviewed SHA. Image family, OS version and architecture
  are checked; actual ImageOS/ImageVersion and native platform are recorded.
  A new exclusive attempt directory prevents repeating the entry within a job;
  cleanup retains its consumed marker. This does not authorize multiple fresh
  workflow dispatches: the tester must record the single run ID before monitoring.
- One fixed build sequence, Windows missing-envelope control, one baseline,
  Unix `/usr/bin/true` startup and at most one sandbox probe. Baseline failure or
  startup rejection ends that platform before the next sandbox launch. The nine
  original relations are unchanged. Parsing now rejects malformed/unknown/extra
  fields as well as duplicate records instead of accepting arbitrary third text.
- Linux records bwrap path/mode/hash, binary and dpkg package versions, the parent
  AppArmor label and two named namespace policy fields, read-only. macOS records
  generated canonical path, file architecture, codesign display and otool loader
  dependencies without changing its `.sb`. Windows adds generated executable and
  working-directory type/final-path checks plus `GetBinaryTypeW` immediately
  before launch. Error 2 is still not asserted to mean a missing profile.
- Windows preserves profileless/no-capability/no-inherited-handle/empty-environment
  launch. A kill-on-close Job owns the suspended child before resume; assignment
  failure never resumes it or falls back. The existing 20-second child wait and
  one final at-most-5-second termination wait remain bounded. Quiescence/cleanup
  failures end controller work. This new native path is not yet compiled or
  exercised on Windows and must be reviewed as unverified instrumentation.
- The controller caps each captured pipe at 16 KiB and subprocess execution at
  60 seconds; overflow is failure, never a silently truncated successful record.
  Unix process-group leftovers are killed and invalidate the capture. Windows
  controller timeouts target only its own compiler/launcher tree; the native
  launcher owns the fixed probe through its Job. No descendant stress controls
  or arbitrary untrusted subprocess support have been added.
- Only fixed self-authored sentinel content and `probe.c` writes are permitted,
  comfortably below the 1 MiB control-payload allowance. A probe binary over
  4 MiB is rejected before copying/launch. Inventory counts directories and
  links conservatively within 64 entries and monitors 64 MiB total scratch;
  compiler temporary directories are redirected to this private root. Inventory
  monitoring is **not an atomic OS disk quota** and must not be reused to contain
  arbitrary writers. The trusted fixed toolchain and tiny fixed source remain
  part of this diagnostic trust contract; no general disk/resource qualification
  is claimed. The static reviewer must assess this bound before permitting the
  one run, rather than infer permission from passing mock tests.
- Collection names are controller-selected regular files; known probe links are
  never traversed. Raw bytes are SHA-256-bound and base64-encoded behind
  `B04_RECORD `, with an all-or-nothing 1 MiB encoded bundle cap. Nothing writes
  GITHUB_ENV/GITHUB_OUTPUT or emits native text as workflow commands. On supervisor
  failure only bounded parent-held pipe prefixes are emitted, explicitly marked
  `unqualified-prefix/`; no further filesystem collection/cleanup occurs in that
  guest. Ordinary completed fixed runs remove only their owned data without
  following links, verify removal, and retain the attempt marker. No accepted
  evidence is recursively unpacked or executed.

No safe process-specific audit/crash retrieval source has yet been established.
The entry explicitly reports that absence instead of dumping system logs that
may contain unrelated job/credential information. Identity observations may still
leave the exact macOS denial or Linux capability cause unresolved. Such a result
is a blocker at the end of the authorized round, not grounds for an automatic
trace-driver install, broad log read, permission expansion, or another run.

Workflow `.github/workflows/b04-native.yml` is manual-only, with explicit standard
`ubuntu-24.04`, `windows-2025-vs2026`, `macos-26`, `max-parallel: 1`, and a 10-minute
job deadline. Checkout/setup-python are pinned to their read-only-resolved v4/v5
commit SHAs; credential persistence is disabled. No new artifact upload, cache
save, package or external storage is configured. PUBLIC/not-archived and Actions
enabled were rechecked with read-only gh; recheck eligibility immediately before
the authorized execution. No paid/larger runner or personal computer is used.

Other existing workflows still trigger on every branch push. Therefore the
preparation commit includes `[skip ci]`, per the official
[skip documentation](https://docs.github.com/en/actions/managing-workflow-runs-and-deployments/managing-workflow-runs/skipping-workflow-runs),
to avoid spending unapproved jobs when the reviewed branch is later published.
This applies to push/PR events, not the selected workflow_dispatch. Do not modify
the other workflows or default branch. After stage PASS only: publish the exact
reviewed head, verify remote SHA and absence of unintended push runs, then use
the registered workflow's API/CLI ref path with `approved_sha` equal to that
head. Dispatch at most once; record and verify run/head/attempt/job matrix. Do not
rerun even a setup failure. A skipped/Pending general check is not a CI PASS;
no merge or release is requested here.

Offline preparation verification (no new native/sandbox/CI execution):

- `python3 .github/tests/migration/b04/test_prerequisite.py`: seven offline tests
  pass, one explicit native test skipped (eight discovered). RED-to-GREEN probes
  demonstrated the previous unwanted launch after startup/baseline failure and
  malformed-record acceptance using injected synthetic unit data, never goldens.
- `python3 .github/tests/migration/b04/test_diagnostic.py`: offline controller,
  identity, cap, encoded transport, no-follow collection, cleanup and repeat
  rejection tests. The full controller test uses the real capture/transport/
  cleanup with injected subprocess results; it is NOT native evidence.
- `cc -std=c11 -Wall -Wextra -Werror -fsyntax-only
  .github/tests/migration/b04/probe.c`: exit 0; no binary was executed. Native
  Windows compiler, cross-compiler and actionlint were not available locally.
  No production/native archive build is appropriate before this stage review.
- Offline DAG validation and the four existing release-version, change-notice,
  language-file and shell-extension-language checks passed. Both historical
  manifests and protected-source preservation are rechecked in the same-card
  handoff, with exact final commands/results and commit identity.

All existing native evidence and expectations remain untouched. No new extraction
tree/payload/metadata/overwrite/EOF/cancel/null-stream/partial-effect compatibility
evidence exists; no permission, codec, GUI, legacy engine or production policy
change was made. Review the new controller and C lifecycle cold, including the
fixed-tool resource assumptions and failure cleanup, before any native launch.

References:
- [Migration rules](../../../AGENTS.md)
- [B04 prerequisite runner](../../../.github/tests/migration/b04/prerequisite.py)
- [Native workflow](../../../.github/workflows/b04-native.yml)
- [Microsoft SID API](https://learn.microsoft.com/en-us/windows/win32/api/userenv/nf-userenv-deriveappcontainersidfromappcontainername)
