# B03 — native metadata, large files and partial I/O

## Status: authorized extra CI failed; stopped, no qualification PASS

Run 34571301200 completed with overall failure at the exact repair HEAD
`500eecdb435d03580ca8f640f72becfb617bff02`:
https://github.com/r404r/7zip/actions/runs/34571301200.
This consumed the one additional implementation-side three-OS CI round.
No further repair, push, workflow dispatch or rerun was performed after failure.
The later evidence/documentation commit is local-only to avoid another push
trigger. This card remains incomplete and must not release descendants or enter
qualification review as if the native gate had passed.

Actual outcomes:

- Linux: both builds, capture/repeat, six negative controls, validation and
  freeze pass. The downloaded frozen manifest independently verifies 43 files.
  Native `stat` reports `ext2/ext3`; this label is retained, not silently renamed
  ext4. Small/large sparse guards and the complete >4 GiB ZIP64 stream pass.
- Windows and macOS: both builds pass. Each capture contains six metadata
  round trips, five property probes and twelve fault runs, with real per-file
  native ACL observations and typed handler properties. Native main filesystem
  identity is `NTFS` and `apfs`, respectively. These partial observations have
  not passed repeat comparison or independent review.
- Both fresh image facilities work without privilege/policy changes: Windows
  NTFS VHD measured total 65990656 bytes; macOS HFS+ image measured total
  67067904 bytes. Both successful archive creation/testing controls precede
  actual filler errno 28. Exact native detach commands returned 0; raw messages
  confirm the VHD detached and macOS `"disk7" ejected.`
- Both captures fail at `full_volume.py:125` before the intended archive-error
  command. Windows reports filled size 53477376 and remaining space 167936;
  macOS reports filled size 65011712 and remaining space 499712. Both exceed
  the 65536-byte input. The recipe incorrectly treated failure of a 1 MiB write
  as complete volume exhaustion, although a smaller write could still fit.
  Releasing another 16 KiB does not establish the intended shortage. The guard
  correctly rejects this experiment; do not remove it or count Python filler
  ENOSPC as a retained archive-write error. The `kernel_full.layer` field names
  the intended layer, **not a reached/qualified layer** in these failed reports.
- Windows/macOS stop before sparse/ZIP64 in this round. Thus the changed macOS
  truncate-first recipe is still unverified; it is not a proven repair of the
  earlier sparse allocation failure. Both new reports remain incomplete.

Original error (unchanged, both native failures):

```text
    assert record['free_before_archive'] < source.stat().st_size
AssertionError
```

The full raw run/job/artifact metadata, reports, available archive bytes,
compiler logs, original GitHub failure logs (existing `***` redactions retained)
and a read-only `jq` projection are preserved in
[34571301200.tar.gz](../../../.github/tests/migration/b03/evidence/ci-attempts/34571301200.tar.gz),
with its digest in the existing SHA256SUMS manifest. Unlike the previous failed
attempts, this workflow retained completed metadata/fault archive bytes even
when freeze was skipped. No Windows/macOS repeat report or qualification PASS
was fabricated. Disk images/filler are not included in the bundle.

Executed post-CI commands (all exit 0 except watch, which exits 1):

```text
gh run watch 34571301200 -R r404r/7zip --exit-status --interval 30
gh run download 34571301200 -R r404r/7zip --dir .github/tests/migration/b03/work/ci-34571301200
gh run view 34571301200 -R r404r/7zip --json headSha,status,conclusion,jobs,url
gh run view 34571301200 -R r404r/7zip --log-failed
gh api repos/r404r/7zip/actions/runs/34571301200/artifacts
python3 .github/tests/migration/b03/freeze.py .github/tests/migration/b03/work/ci-34571301200/b03-ubuntu-latest-500eecdb435d03580ca8f640f72becfb617bff02/frozen
tar -czf .github/tests/migration/b03/evidence/ci-attempts/34571301200.tar.gz -C .github/tests/migration/b03/work ci-34571301200
sha256sum -c .github/tests/migration/b03/evidence/ci-attempts/SHA256SUMS
```

Next decision, not implied authorization: recommended independent review of
the bounded native probe recipe and an explicitly budgeted targeted native
preflight before any further full B03 CI. The remaining free-space tail must be
exhausted at a smaller bounded write granularity and the native sparse recipe
must be qualified, without weakening either guard. Alternatively provide a
reviewed native fixture/probe or runner execution that returns those raw results.
Both options preserve all acceptance gates; neither authorizes a release,
shared-branch merge, changed engine behavior, golden rewrite or new spending.

## Authorized bounded repair implementation and local preflight (historical)

The operator explicitly approved one additional implementation-side three-OS CI
round on the same card. This does not reset the global retry policy or authorize
a reduced acceptance criterion. If that round fails, preserve evidence and stop.
Commit `b0623c36433abc46bd7f070102f7b2e6ef28d649` and both previous raw CI bundles
remain untouched ancestors/evidence. No engine, golden, shared workflow or
security policy is changed.

Repair scope and evidence:

- `probe.inc` no longer reads reserved storage on non-`VT_FILETIME` values.
  `PropVariant.h:60-67,118-130` and `PropVariant.cpp:195-196` leave scalar words
  2/3 undefined. For time precision 0, `PropID.h:146-148` also explicitly permits
  random words 2/3; the sampler labels those undefined words `null`, not zero.
  All three words of explicit time precision, VARTYPE, FILETIME and HRESULT stay
  in the comparison. The comparator interprets old reports using that same
  tagged contract without modifying their bytes. Replaying the actual second
  Windows capture/repeat first failed with the original exact assertion, then
  passed; independent mutations of each defined time word and HRESULT fail.
- Native filesystem identity uses `df -P` device lookup plus `diskutil info
  -plist` on macOS, GNU filesystem stat on Linux, and `GetVolumeInformationW`
  on Windows. No APFS identity is inferred from an OS name or BSD `%T`.
- The sparse recipe now sets EOF with truncate before unbuffered endpoint
  writes and fsync. This tests a different native allocation path, not a new
  portable engine guarantee. The 16 MiB allocation limit remains strictly
  below 1 MiB. Actual allocation, recipe and free capacity are saved even if
  the allocation guard fails. Windows uses `GetCompressedFileSizeW`, not just
  the sparse attribute. A >4 GiB input requires a live same-directory small
  proof, and free space exceeds the full logical size plus 512 MiB. Failure
  never proceeds to large-file creation. Native macOS confirmation is pending.
- File and directory ACL capture now includes native Windows owner/group/DACL
  SDDL and macOS `ls -lde` entries before and after extraction. A deliberate
  deny-delete ACE is confined to `fractional.txt` in disposable input; its
  presence is checked. Raw macOS command output is retained, with only the
  first volatile path/stat line excluded from the ACL comparison. No SACL,
  privilege elevation or portable ACL mapping is claimed.
- `full_volume.py` provisions only fresh 64 MiB Windows VHD or macOS HFS+
  image sandboxes, using existing native facilities with no policy change.
  It never selects a physical disk or formats an existing image. Mount identity,
  separate device/volume, image-sized capacity and host free space are checked
  before filling. Writes have a hard 65 MiB ceiling, even with bad free-space
  reporting. A successful create/test control precedes real ENOSPC; then
  retained create/update execute with only 16 KiB released from the filler.
  Numeric filler errors, actual CLI diagnostics and failed-output bytes are
  retained. Cleanup targets only that image/mount and never force-detaches.
  An unavailable facility fails qualification rather than masquerading as
  full-disk behavior. Windows/macOS execution is still pending.
- New reports use schema 2, requiring native filesystem/sparse evidence and
  Windows/macOS file ACL/full-volume evidence. Schema 1 remains readable only
  for historical evidence. New negative controls reject missing filesystem,
  allocated sparse input, removed native ACL and unrelated full-volume errors.
  The workflow now uploads bounded archive/retained bytes even on failure;
  disk images, filler and large logical inputs are excluded.

Local commands executed successfully before the single authorized CI round:

```text
python3 .github/tests/migration/b03/test_integrity.py -v
python3 .github/tests/migration/b03/build.py .github/tests/migration/b03/work/authorized-repair
python3 .github/tests/migration/b03/run.py .github/tests/migration/b03/work/authorized-repair --report .github/tests/migration/b03/work/authorized-repair/final-capture.json
python3 .github/tests/migration/b03/run.py .github/tests/migration/b03/work/authorized-repair --report .github/tests/migration/b03/work/authorized-repair/final-repeat.json --expect .github/tests/migration/b03/work/authorized-repair/final-capture.json
python3 .github/tests/migration/b03/validate.py .github/tests/migration/b03/work/authorized-repair/final-capture.json
git diff --check
git diff --exit-code b0623c3 -- C CPP Asm AGENTS.md
```

Both local builds, capture/repeat, six report mutations and nine regression
tests pass. The local build HEAD is `b0623c3`; modified harness hashes are recorded
separately, not falsely claimed to be already in that commit. Native CI must
build the committed repair HEAD. Native full-volume facilities are not present
on this Linux host, so those paths are not locally certified. The host-directory
refusal and missing-small-proof tests execute here without filling a host disk.

The new local reports and archive bytes are frozen separately in
[local-linux-repair](../../../.github/tests/migration/b03/evidence/local-linux-repair/manifest.json).
`python3 .github/tests/migration/b03/freeze.py .github/tests/migration/b03/evidence/local-linux-repair --work .github/tests/migration/b03/work/authorized-repair --capture .github/tests/migration/b03/work/authorized-repair/final-capture.json --repeat .github/tests/migration/b03/work/authorized-repair/final-repeat.json`
verified 43 immutable files and six negative controls. The six existing
repository preservation checks listed in the historical reproduction section
were rerun successfully. No historical evidence was replaced.

## Historical status: native CI attempted twice; incomplete, not qualification PASS

The human approved the recommended isolated workflow activation on this card.
`.github/workflows/b03-native.yml` is now active only for this task branch (or
manual dispatch); no existing workflow was edited. The earlier scope gate below
is historical, not a request to approve credentials or the same path again.

Native runs (both completed, both overall **failure**):

- Run 34568438732: https://github.com/r404r/7zip/actions/runs/34568438732,
  HEAD `dcc4d32fd222e319b61150d300c6841032552427`: Ubuntu passed build,
  capture/repeat, validation, negative controls and freeze. Windows built both
  executables but the harness's post-build provenance check failed: its
  modified-file keys used backslashes while Git names use slashes. macOS built
  both executables but its Python `os` module has no `setxattr`.
- Run 34568709582: https://github.com/r404r/7zip/actions/runs/34568709582,
  HEAD `e886a32337f15540fcb368ca9f1b23d598666b0f`: the path-key fix
  (`as_posix()`) and native macOS `xattr` command fixed those specific failures.
  Ubuntu again passed every B03 step. Windows passed build and full capture,
  then failed repeat comparison. macOS passed build, six metadata cases, five
  property probes and twelve fault cases, then the sparse-allocation safety
  assertion stopped the run before the >4 GiB fixture was created.

Exact unresolved errors:

```text
AssertionError: Stable native observations differ: ['cases', 'large_properties']
AssertionError: Sparse source unexpectedly allocated
```

`diagnose.py` compares the original Windows reports read-only. The differences
are `reserved[1]` on non-time properties of ZIP cases and ZIP64 properties.
`CPP/Windows/PropVariant.h:60-67` leaves `wReserved2`/`wReserved3` uninitialized;
`CPP/7zip/PropID.h:142-161` separately defines timestamp precision and explicitly
allows random reserved data in base precision mode. This is a harness contract
problem, not evidence of a changed archive engine. The comparator and original
observations have **not** been weakened or rewritten to obtain green CI.
Any repair must stop interpreting undefined non-time storage while retaining
defined timestamp precision, with regression/negative-control evidence.

macOS `stat -f %T` captured `/`, not an adequate filesystem-type identifier.
The next attempt must obtain native filesystem type and actual allocation
evidence, then validate a bounded native sparse fixture before any >4 GiB write.
Do not simply remove the allocation assertion or claim APFS from the OS label.
macOS ACL setup succeeded but post-extraction native ACL comparison remains a
coverage gap; Windows `icacls` currently inventories directories rather than a
deliberately differentiated file DACL. Kernel-full-volume coverage on those
platforms also remains absent. These limits prevent qualification PASS.

Both downloaded attempts are preserved losslessly in
[ci-attempts](../../../.github/tests/migration/b03/evidence/ci-attempts/SHA256SUMS).
The archives contain original artifact reports/compiler logs; the second also
contains GitHub run/job and artifact metadata, the read-only stable diff, and
GitHub failure logs (GitHub's original `***` redactions remain redacted).
These are **failed-attempt diagnostics, not golden data**. The second Ubuntu
frozen manifest independently verified 43 files and four negative controls.
Failed Windows/macOS artifacts do not contain archived fixture bytes because
the freeze step was correctly skipped; they cannot stand in for complete native
qualification. No synthetic output fills that gap.

Commands actually executed after authorization:

```text
python3 .github/tests/migration/b03/test_integrity.py -v
git diff --check
git push -u origin wt/t_bf92ce13
gh run watch 34568438732 -R r404r/7zip --exit-status --interval 30
gh run download 34568438732 -R r404r/7zip --dir .github/tests/migration/b03/work/ci-34568438732
git push origin wt/t_bf92ce13
gh run watch 34568709582 -R r404r/7zip --exit-status --interval 60
gh run download 34568709582 -R r404r/7zip --dir .github/tests/migration/b03/work/ci-34568709582
python3 .github/tests/migration/b03/diagnose.py .github/tests/migration/b03/work/ci-34568709582/b03-windows*/capture.json .github/tests/migration/b03/work/ci-34568709582/b03-windows*/repeat.json
python3 .github/tests/migration/b03/freeze.py .github/tests/migration/b03/work/ci-34568709582/b03-ubuntu*/frozen
sha256sum -c .github/tests/migration/b03/evidence/ci-attempts/SHA256SUMS
git diff --exit-code 2eeeb00 -- C CPP Asm AGENTS.md
```

The four integrity tests passed on both local invocations; both `gh run watch`
commands returned 1 for genuine native failures. Freeze verification, checksum
verification, diagnosis and preservation checks returned 0. The source checkout
and reviewed parent ancestry were reverified before work. Only the task branch
was pushed; no shared branch merge, release, or additional spend occurred.

After two unsuccessful native qualification runs, stop for explicit retry-bound
review rather than silently continuing a repair loop. Recommended: authorize a
bounded same-card repair attempt addressing the specific native harness/coverage
issues above, keeping all gates and original evidence. Alternative: provide a
reviewed native fixture/probe implementation or runner endpoint and keep this
card blocked until its raw evidence can be consumed. Neither option narrows B03
acceptance. Do not request implementation review or release descendants yet.

## Historical local-only handoff (before workflow authorization)

Task `t_bf92ce13`, branch `wt/t_bf92ce13`. Worktree starts at
`2eeeb0084ef1fd3c4e7abd8282c969ea5b7ab86a`; reviewed parent
`0706af3d714e8c2701691a19a0c663ae7b749c88` is already an ancestor. Source checkout
was verified on `ai/migration-bootstrap-20260911`; worktree `AGENTS.md` was read.

Only `.github/tests/migration/b03/` and this document are changed. No production
source, codecs, encryption, M1 harness/goldens, existing workflow, shared branch,
GUI, or architecture is changed. Retained C++ remains owner of input streams,
scanning, archive update, metadata application, and cleanup policy.

**B03 is incomplete and must not release dependent implementation cards.**
Native Windows and macOS B03 executions are still required, as is native CI on
Linux. The existing general workflow does not invoke B03; the B02 workflow is
restricted to B02. A proposed isolated workflow lives at
[tests/migration/b03/native-workflow.yml](../../../.github/tests/migration/b03/native-workflow.yml),
where GitHub does **not** activate it. Activating a new
`.github/workflows/b03-native.yml` is outside the card's named ownership paths;
scope confirmation was requested on parent `t_82c76197`. No approval is inferred
from credentials, notifications, or prior B02 authorization.

The proposed runner does not provision a full Windows/macOS volume. Its
stream-boundary disk-full injection is explicitly not kernel-full-volume
qualification. A native filesystem-qualified report and independent same-card
review must determine the supported/unsupported boundary before B03 is accepted.

## Actual local evidence

[Candidate manifest](../../../.github/tests/migration/b03/evidence/local-linux/manifest.json)
contains 43 losslessly compressed files: capture/repeat JSON, complete build
provenance and both compiler logs, plus every generated `.7z`, `.zip`, and `.tar`
from the two final capture sandboxes. It retains exact archive bytes, including
empty outputs and unchanged old archives after failures. Both stored and original
SHA-256/size are checked. Candidate data are **unreviewed**, not portable goldens.

Environment: Linux x86_64, kernel `7.0.0-28-generic`, glibc 2.43, GCC
`15.2.0-16ubuntu1`. Native CLI banner reports `en_US.UTF-8`; the report separately
retains Python locale, timezone and umask. Session `df -T .` identified ext4;
`stat -f -c %T` in the raw capture reports its Linux filesystem-family label
`ext2/ext3`. These are not evidence for FAT, NTFS, APFS, SMB, or other volumes.

Unchanged executable SHA-256:
`42349815b23dd2fe2adc4d712cd9275754012c43576438ab4c24a76589575646`.
Test-only executable SHA-256:
`d5570b8af1b863d2b551b2e81105d62ae392ec5f92a07ff655e6d3f1587f34fa`.

`build.json` records the actual source HEAD and hashes of every copied tracked
`C/`, `CPP/`, `Asm/` file, exact build/compiler command arrays and executable
hashes. The build occurred before the test files were committed; capture JSON
therefore also records exact harness-file hashes. Do not misrepresent the build
HEAD as containing then-untracked test instrumentation.

## Method and bounded effects

`build.py` copies tracked engine and assembly sources to a fresh directory,
asserts no engine difference from reviewed `d9c3b65`, and builds unchanged Alone2.
The second build uses separate objects and changes only the disposable copies of
`UI/Console/Main.cpp` and `Common/FileStreams.cpp`, with two included test files.
All other source hashes are checked after the build.

`probe.inc` directly opens real archive files using `CInFileStream`, then real
7z/ZIP/TAR handlers. It records open/count/property/close HRESULTs, engine indices,
VARTYPE, all three reserved precision words, FILETIME low/high words, numeric
attributes/size, bools, and BSTR wchar units. COM owners release before the input
stream owner; no borrowed property pointers escape. `wchar_bytes` is explicit.
Raw time type/precision must not be reduced to formatted timestamps in M2.

`fault.inc` is an opt-in test-only seam at `CInFileStream::Read` and
`COutFileStream::Write`. It caps requested chunks at seven bytes; error modes
allow 4096 requested bytes before returning the platform-mapped read-fault or
disk-full HRESULT. All permitted operations still use real retained native I/O.
The counter measures **permitted**, not necessarily OS-completed, bytes. It is
process-local and designed for these single-input/output `-mmt=off` commands, not
concurrent fault injection. Raw stderr proves activation and error triggering.
Normal probe-CLI listings match unchanged CLI listings byte-for-byte. This
checks the no-fault path; it is not a claim that injected failures are naturally
occurring kernel events or that every native wrapper is independently qualified.

`run.py` uses new worktree-local sandboxes and never reads user archives. Inputs
are authored deterministic payloads, not authored expected outputs. A 16 MiB
sparse fixture characterizes output allocation. The >4 GiB input has only two
written extents; Windows must successfully set `FSCTL_SET_SPARSE` before seeking.
Unix source allocation is bounded to less than 1 MiB. The large compressed
archive must be below 64 MiB; decompression streams through an 8 MiB hash buffer
rather than allocating a 4 GiB destination. Each regular CLI command has a
timeout, and the proposed native job has a 35-minute overall bound. A hung
streaming decompressor still relies on that enclosing job/process timeout.
Sandboxes are retained for diagnosis, not recursively deleted by this harness.

## Measured Linux results

- Six metadata round trips: 7z/ZIP/TAR, each with `TZ=UTC0` and `TZ=EST5EDT`.
  Creation, listing and extraction all return 0; all payload hashes survive.
- Input mtime `1700000001123456789` becomes `1700000001123456700` for 7z/ZIP and
  `1700000001000000000` for default TAR. This observation includes the directory
  mtime after child extraction. The two tested timezone settings give the same
  extracted mtimes; this does not characterize every historical/DST timestamp.
- Executable mode `0755`, readonly `0444`, directory `0750`, and ordinary file
  modes survive on this Linux volume. Windows readonly semantics are not inferred.
- `user.b03` xattr and a named POSIX ACL are successfully applied to the input.
  Default extracted ordinary files have empty xattr inventories; neither the
  user xattr nor `system.posix_acl_access` is preserved. This is an observation,
  not an approved new portable ACL mapping.
- Separate `-sni`, `-sns`, and combined `-sni -sns` 7z creation probes each return
  exit 2 with `E_NOTIMPL : Not implemented`. Invalid ZIP method property returns
  exit 2 with `E_INVALIDARG : One or more arguments are invalid`. The times-on
  7z probe succeeds and records raw time properties. CLI labels are not numeric
  handler HRESULTs; only the direct property probe records those numerically.
- Twelve fresh-create/update runs cover no fault, short read, short write,
  read error, disk-full injection, and missing input. Short I/O runs succeed and
  produce the exact same archive SHA-256 as their no-fault control.
- Fresh-create read/write failure returns 2 and leaves no archive. Missing input
  returns warning exit 1 and leaves a 22-byte empty ZIP. Do not flatten warning
  into either complete success or no effects.
- Update read/write failure returns 2 and retains the byte-identical 165-byte
  original archive; missing input returns 1 and likewise retains it. These early
  failures do **not** prove rollback/atomicity for later rename/cross-volume
  replacement failures.
- Real Linux `/dev/full` receives an unchanged-CLI extraction stdout stream,
  returning exit 2 with `errno=28 : No space left on device`. The host disk is
  never filled. This is `CStdOutFileStream` kernel evidence, distinct from
  `COutFileStream` injected archive-output failures.
- The 16 MiB sparse source has `st_blocks=16`; default retained extraction has
  `st_blocks=32768`, with identical content SHA-256. Sparse logical contents
  survive, but this operation does not preserve sparse allocation.
- Actual ZIP64 source/extracted size is `4294967329`. Central directory reports
  extract version 45 and ZIP64 extra ID `0x0001`. Archive size is 5040025 bytes.
  The entire extracted stream matches source SHA-256
  `6dcc95374a10eaf4726e149603bc3727d89004f440c9a0879580f9132044ff2d`.
- Direct retained native seeks cover negative absolute offset, offset
  `4294967313`, EOF, beyond EOF, and invalid origin. Error-position output is
  labeled undefined, never used to infer a portable position guarantee.

## Source anchors and compatibility boundary

- `CPP/7zip/PropID.h:136–175`: FILETIME representation, precision/reserved words,
  Linux ctime meaning, and update-time precision contract.
- `CPP/7zip/IStream.h:23–105`: partial read/write, EOF, signed seek and undefined
  error-position contracts.
- `CPP/7zip/Common/FileStreams.cpp:354–420,472–537,589–795,802–918`: actual native
  stream seek/size, raw properties, read/write and stdout error conversion.
- `CPP/7zip/Common/StreamUtils.cpp:54–100`: caller loops for partial I/O.
- `CPP/7zip/UI/Common/Update.cpp:463–468`: archive capabilities reject unsupported
  stream/security opt-ins; `:1617–1679` explicitly distinguishes later move
  failure from the earlier cleanup measured here.
- `CPP/7zip/UI/Common/TempFiles.cpp:12–19`: retained temporary-file deletion.

Keep raw native names and errors, timestamp type/precision, warning versus
failure, and failed-output dispositions in downstream interfaces. Do not turn
these early-update observations into a transactional promise. No production
filesystem replacement, GUI, desktop integration, privilege escalation, or new
security/overwrite policy is authorized by this candidate evidence.

## Reproduction and executed checks

From the repository root, exact successful local commands:

```text
python3 .github/tests/migration/b03/build.py .github/tests/migration/b03/work/local
python3 .github/tests/migration/b03/run.py .github/tests/migration/b03/work/local --report .github/tests/migration/b03/work/local/final-capture.json
python3 .github/tests/migration/b03/run.py .github/tests/migration/b03/work/local --report .github/tests/migration/b03/work/local/final-repeat.json --expect .github/tests/migration/b03/work/local/final-capture.json
python3 .github/tests/migration/b03/validate.py .github/tests/migration/b03/work/local/final-capture.json
python3 .github/tests/migration/b03/freeze.py .github/tests/migration/b03/evidence/local-linux --work .github/tests/migration/b03/work/local --capture .github/tests/migration/b03/work/local/final-capture.json --repeat .github/tests/migration/b03/work/local/final-repeat.json
python3 .github/tests/migration/b03/freeze.py .github/tests/migration/b03/evidence/local-linux
python3 .github/tests/migration/b03/summarize.py .github/tests/migration/b03/work/local/final-capture.json
python3 .github/tests/migration/b03/test_integrity.py -v
```

Use fresh build/report/freeze paths when recapturing; overwrites are rejected.
Both native builds exit 0. Final capture/repeat/completeness checks pass. Four
independent temporary report mutations (mtime, CLI exit, ZIP64 size, raw property
type) are rejected. The comparator preserves raw reports, omitting only volatile
ctime/atime *values*, directory size/allocation, run paths and console text from
stable comparison. It retains time VARTYPE/precision and compares payload/name,
mtime/mode/attributes, direct properties, statuses and output dispositions.
The raw evidence remains authoritative for observations outside that projection.
Four integrity regression tests also pass: frozen candidate verification, actual
compressed-archive byte corruption rejection in a disposable copy, rejection of
an unrelated nonzero exit as ENOSPC evidence, and document relative-link checks.

An initial `/dev/full` test incorrectly attempted ZIP creation to stdout and got
`E_NOTIMPL`, not ENOSPC. A tightened validator demonstrably rejected that report
before the harness was corrected to extract an existing ZIP to stdout. The
final captured evidence observes actual errno 28. Initial scratch reports remain
in `work/local/` for diagnosis and are **not** frozen as qualifying evidence.
No expected observation was edited to make this pass.

Existing repository checks were also executed successfully:
`release_version_test.py`, `change_notice_test.py "$PWD"`,
`lang_files_test.py "$PWD"`, `shell_ext_identity.py "$PWD"`,
`shell_ext_lang_reload.py "$PWD"`, and `bash .github/tests/release_notes_test.sh`.
Preservation check against task start `2eeeb00` confirms no changes to `C`, `CPP`,
`Asm`, `AGENTS.md`, or `.github/workflows`. Comparing workflows against older
`d9c3b65` instead shows the already-reviewed B02 addition, not a B03 change.

## Remaining gate and review

Options requested on the card:

1. Authorize only the new isolated B03 workflow path (recommended), run actual
   Windows/Linux/macOS builds/capture/repeat, freeze their exact artifacts, then
   request same-card `reviewer` review. Existing shared workflows stay untouched.
2. Provide an existing authorized native Windows/macOS execution endpoint that
   can run the same commands and return provenance/raw artifacts. Repository
   workflow ownership stays unchanged; external runner access is required.
3. Explicitly narrow this card to Linux-only partial characterization. This does
   not qualify Windows/macOS; affected descendants must remain gated until
   independently reviewed native coverage is supplied.

There is no missing GitHub credential: `gh auth status` succeeded. The issue is
scope/coverage, not permission to activate an already supplied credential.
Documentation and test changes remain unreviewed until the actual coverage gate
is resolved. Never archive this card or self-complete it to release downstream
work. Once implementation and all required native evidence are ready, use
same-card `kanban_request_review(reviewer="reviewer")`; all four current children
are implementation cards, not pre-created independent-review children.
